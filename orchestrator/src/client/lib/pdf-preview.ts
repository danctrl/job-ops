// Shared zoom bounds for canvas-based PDF previews (DesignResumePdfPreview and
// the settings cover-letter preview) so both step/clamp identically.
export const PDF_ZOOM_MIN = 0.5;
export const PDF_ZOOM_MAX = 2;
export const PDF_ZOOM_STEP = 0.1;
